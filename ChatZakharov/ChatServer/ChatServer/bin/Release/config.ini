internal_ip=127.0.0.1
port=1815
connection_timeout_ms=5000
first_run=False
db_connectionString=(Server=127.0.0.1;Port=9999;User Id=postgres;Password=password;)

// Configure db_connectionString before first run;

// IMPORTANT first_run will drop and recreate chat database if it sets to true.
// After success db creation it will automatically switch to false;
